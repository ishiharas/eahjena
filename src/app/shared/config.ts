const baseUrl: string = "http://193.174.232.89:8080/fheapp/api/eah/"

const courses: string = baseUrl + "timetable/";
export { courses };

const eventQuery: string = "events?timetableId=";
export { eventQuery };

const canteens: string = baseUrl + "canteens/";
export { canteens };

const news: string = baseUrl + "news/1";
export { news };

const weather: string = baseUrl + "weather/";
export { weather };

const imprint: string = baseUrl + "impress/";
export { imprint };
