export enum LSOBJECTS {
    LASTMOD = 'lastMod',
    LASTUPDATED = 'lastUpdated',
    MODULEIDS = 'moduleIds',
    MODULEPLAN = 'modulePlan',
    ADDITIONALMODULES = 'additionalModules' //  { courseID: string, moduleId: string[], courseShortString?: string}[]
}
